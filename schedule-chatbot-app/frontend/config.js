// ถ้า frontend กับ backend อยู่คนละที่ (deploy แยกกัน) ให้ใส่ URL ของ backend ตรงนี้
// เช่น "https://your-backend.onrender.com"
// ถ้า frontend ถูก serve จาก backend เดียวกัน (ค่าเริ่มต้นตามคู่มือ) ให้ปล่อยเป็นค่าว่าง
window.API_BASE = "";
